// A function that takes two parameters, the second one being a callback function

const getInput = (objet, rappel) => {
  rappel (objet.key);
}; 




// code the function here



// Leave the line below for tests to work
module.exports = getInput;
